# Django development environment
DEBUG=True

# PostgreSQL database settings
DB_NAME=feedback_db
DB_USER=postgres
DB_PASSWORD=your_postgres_password
DB_HOST=localhost
DB_PORT=5432

# Optional: full database URL (if set, overrides the individual DB_* variables above)
# DATABASE_URL=postgres://postgres:your_postgres_password@localhost:5432/feedback_db
